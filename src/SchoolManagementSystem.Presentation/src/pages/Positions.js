import React from "react";
import NavBar from "../components/NavBar/NavBar";

const Positions = () => {
    return (
        <div>
            <NavBar></NavBar>
        </div>
    );
};

export default Positions;